// =============================================================================
// Polling Intervals
// =============================================================================
// Centralized timing constants for visibility-based polling

/** Tunnel status polling interval (ms) */
export const TUNNEL_POLL_INTERVAL = 10000; // 10 seconds

/** Presence heartbeat interval (ms) */
export const PRESENCE_HEARTBEAT_INTERVAL = 30000; // 30 seconds
